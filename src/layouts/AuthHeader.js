import React from 'react';
import {View} from 'react-native';

import {Header, Body, Text, H1} from 'native-base';

const AuthHeader = () => {
  return (
    <Header>
        <Body>
            <H1>Home</H1>
        </Body>      
    </Header>
  );
};

export default AuthHeader;
